version = "1.0.0"

description =
        "Use custom emojis without Nitro"

aliucord {
    changelog.set("""
        # 1.0.0
        - Initial release
        - Added emote size customization

        # Changes from original nitrospoof
        - Added multiple format types - URL (original), markdown, extended markdown, zero-width space
        """.trimIndent())
}

